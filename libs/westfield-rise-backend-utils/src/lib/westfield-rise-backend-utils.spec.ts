import { westfieldRiseBackendUtils } from './westfield-rise-backend-utils';

describe('westfieldRiseBackendUtils', () => {
  it('should work', () => {
    expect(westfieldRiseBackendUtils()).toEqual('westfield-rise-backend-utils');
  });
});
